{% extends 'base.html.twig' %}

{% block title %}Promotion {{ promotion.annee }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/promotion.css') }}">
{% endblock %}

{% block body %}
<div class="promotion-container">

    <!-- Breadcrumb -->
    <div class="breadcrumb">
        <span></span>
        <span>›</span>
        <span>Années scolaire</span>
        <span>›</span>
        <span>Promotion {{ promotion.annee }}</span>
    </div>

    <!-- Titre -->
    <h1>Promotion {{ promotion.annee }}</h1>

    <!-- Formu -->
    <div class="form-card">
        <div class="form-row">
            <div class="form-group">
                <label>Année - champ obligatoire</label>
                <input type="text" value="{{ promotion.annee }}">
            </div>

            <div class="form-group">
                <label>Saison (format YYYY/YYYY) - champ obligatoire</label>
                <input type="text" value="{{ promotion.saison }}">
            </div>
        </div>

        <div class="form-actions">
            <a href="{{ path('annees_scolaires') }}" class="btn-secondary">
                Retour à la liste
            </a>

            <button class="btn-danger">
                Supprimer
            </button>

            <button class="btn-primary">
                Enregistrer les informations
            </button>
        </div>
    </div>

    <!-- Section pour les semaines -->
    <h2 class="section-title">Semaines de cours</h2>
    <p class="section-desc">
        Les semaines de cours permettent de contrôler l'ajout des interventions.
        Une intervention a lieu uniquement pendant les semaines de cours.
    </p>

    <div class="card">
        <div class="table-header">
            <span>Date début</span>
            <span>Date de fin</span>
            <span></span>
        </div>

        {% for semaine in semaines %}
            <div class="table-row">
                <span>{{ semaine.debut }}</span>
                <span>{{ semaine.fin }}</span>
                <span>
                    <a href="#" class="link">👁 Accéder à la fiche</a>
                </span>
            </div>
        {% endfor %}
    </div>

    <button class="btn-primary add-btn">
        Ajouter une semaine de cours
    </button>

</div>
{% endblock %}